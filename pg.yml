services:
  db:
    image: postgres:16-alpine
    environment: {
      POSTGRES_PASSWORD: gas-db,
      POSTGRES_DB      : gas-db,
      POSTGRES_USER    : postgres
      }
    ports: [ '5441:5432' ]
    restart: always